{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for SwarmForge desktop — minimal access only",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
